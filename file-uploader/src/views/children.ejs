<link rel="stylesheet" href="/styles/children.css">
<main>
    <div class="child title-cont">
        <p>Type</p>
        <p>Name</p>
        <p>Created At</p>
    </div>
    <div class="children-container">
        <% if (locals.children) { %>
            <% children.forEach(elem => { %>
                <% if (elem.type === 'FOLDER') { %>
                    <a href="/folder/<%= elem.id %>">
                        <div class="child">
                            <img src="/images/folder.png" alt="Folder">
                            <p><%= elem.name  %></p>
                            <p><%= new Date(elem.timestamp).toLocaleDateString('en-GB') %></p>
                        </div>
                    </a>
                 
                <% } else { %>
                    <a href="#" class="file-link" data-id="<%= elem.id %>">
                        <div class="child">
                            <img src="/images/document.png" alt="File">
                            <p><%= elem.name  %></p>
                            <p><%= new Date(elem.timestamp).toLocaleDateString('en-GB') %></p>
                        </div>  
                    </a>
                <% } %>
            <% }) %>
         
        <% } %>
    </div>
</main>